<%= form_with(model: subgrupo) do |form| %>
  <% if subgrupo.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(subgrupo.errors.count, "error") %> prohibited this subgrupo from being saved:</h2>

      <ul>
        <% subgrupo.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :name, style: "display: block" %>
    <%= form.text_field :name %>
  </div>

  <div>
    <%= form.label :precio, style: "display: block" %>
    <%= form.number_field :precio %>
  </div>

  <div>
    <%= form.label :descripcion, style: "display: block" %>
    <%= form.text_field :descripcion %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
