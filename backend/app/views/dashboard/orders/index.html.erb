<div class="dashboard-orders">
  <div class="page-header">
    <h1>Gestión de Pedidos</h1>
    <p>Administra todos los pedidos de la pizzería</p>
  </div>

  <!-- Estadísticas -->
  <div class="stats-grid">
    <div class="stat-card">
      <div class="stat-icon">📊</div>
      <div class="stat-content">
        <h3>Total Pedidos</h3>
        <p class="stat-number"><%= @total_orders %></p>
      </div>
    </div>

    <div class="stat-card pending">
      <div class="stat-icon">⏳</div>
      <div class="stat-content">
        <h3>Pendientes</h3>
        <p class="stat-number"><%= @pending_orders %></p>
      </div>
    </div>

    <div class="stat-card paid">
      <div class="stat-icon">✅</div>
      <div class="stat-content">
        <h3>Pagados</h3>
        <p class="stat-number"><%= @paid_orders %></p>
      </div>
    </div>

    <div class="stat-card failed">
      <div class="stat-icon">❌</div>
      <div class="stat-content">
        <h3>Fallidos</h3>
        <p class="stat-number"><%= @failed_orders %></p>
      </div>
    </div>
  </div>

  <!-- Lista de Pedidos -->
  <div class="orders-section">
    <h2>Lista de Pedidos</h2>
    
    <% if @orders.any? %>
      <div class="orders-table">
        <table>
          <thead>
            <tr>
              <th>Número</th>
              <th>Cliente</th>
              <th>Total</th>
              <th>Estado</th>
              <th>Método</th>
              <th>Fecha</th>
              <th>Acciones</th>
            </tr>
          </thead>
          <tbody>
            <% @orders.each do |order| %>
              <tr class="order-row <%= order.status %>">
                <td>
                  <strong><%= order.order_number %></strong>
                </td>
                <td>
                  <div class="customer-info">
                    <div><strong><%= order.customer_name %></strong></div>
                    <div class="customer-details">
                      <%= order.customer_email %><br>
                      <%= order.customer_phone %>
                    </div>
                  </div>
                </td>
                <td>
                  <span class="amount">$<%= number_with_precision(order.total_amount, precision: 0) %></span>
                </td>
                <td>
                  <span class="status-badge <%= order.status %>">
                    <%= order.status.humanize %>
                  </span>
                </td>
                <td>
                  <span class="payment-method">
                    <% case order.payment_method %>
                    <% when 'credit_card' %> 💳 Crédito
                    <% when 'debit_card' %> 💳 Débito
                    <% when 'pse' %> 🏦 PSE
                    <% when 'cash' %> 💵 Efectivo
                    <% else %> N/A
                    <% end %>
                  </span>
                </td>
                <td>
                  <div class="date-info">
                    <div><%= order.created_at.strftime("%d/%m/%Y") %></div>
                    <div class="time"><%= order.created_at.strftime("%H:%M") %></div>
                  </div>
                </td>
                <td>
                  <div class="actions">
                    <%= link_to "Ver", dashboard_order_path(order), class: "btn btn-sm btn-primary" %>
                    <% if order.payment_method == 'cash' && order.pending? %>
                      <%= button_to "Confirmar Pago", confirm_cash_payment_dashboard_order_path(order), 
                          method: :patch, class: "btn btn-sm btn-success", 
                          data: { confirm: "¿Confirmar pago en efectivo?" } %>
                    <% end %>
                    <% if order.pending? || order.processing? %>
                      <%= button_to "Cancelar", cancel_order_dashboard_order_path(order), 
                          method: :patch, class: "btn btn-sm btn-danger", 
                          data: { confirm: "¿Cancelar esta orden?" } %>
                    <% end %>
                  </div>
                </td>
              </tr>
            <% end %>
          </tbody>
        </table>
      </div>
    <% else %>
      <div class="no-orders">
        <p>No hay pedidos registrados aún.</p>
      </div>
    <% end %>
  </div>
</div>

<style>
.dashboard-orders {
  padding: 20px;
  max-width: 1400px;
  margin: 0 auto;
}

.page-header {
  text-align: center;
  margin-bottom: 40px;
  padding: 30px;
  background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
  color: white;
  border-radius: 15px;
}

.page-header h1 {
  margin: 0;
  font-size: 2.5em;
  font-weight: 300;
}

.page-header p {
  margin: 10px 0 0 0;
  opacity: 0.9;
  font-size: 1.1em;
}

.stats-grid {
  display: grid;
  grid-template-columns: repeat(auto-fit, minmax(250px, 1fr));
  gap: 20px;
  margin-bottom: 40px;
}

.stat-card {
  background: white;
  padding: 25px;
  border-radius: 15px;
  box-shadow: 0 5px 20px rgba(0,0,0,0.1);
  display: flex;
  align-items: center;
  gap: 20px;
  transition: transform 0.3s ease;
}

.stat-card:hover {
  transform: translateY(-5px);
}

.stat-icon {
  font-size: 3em;
}

.stat-content h3 {
  margin: 0 0 10px 0;
  color: #666;
  font-size: 1em;
  font-weight: 500;
}

.stat-number {
  margin: 0;
  font-size: 2.5em;
  font-weight: 700;
  color: #333;
}

.stat-card.pending .stat-number { color: #ffc107; }
.stat-card.paid .stat-number { color: #28a745; }
.stat-card.failed .stat-number { color: #dc3545; }

.orders-section {
  background: white;
  padding: 30px;
  border-radius: 15px;
  box-shadow: 0 5px 20px rgba(0,0,0,0.1);
}

.orders-section h2 {
  margin: 0 0 25px 0;
  color: #333;
  font-size: 1.5em;
  border-bottom: 2px solid #667eea;
  padding-bottom: 10px;
}

.orders-table {
  overflow-x: auto;
}

.orders-table table {
  width: 100%;
  border-collapse: collapse;
  font-size: 14px;
}

.orders-table th {
  background: #f8f9fa;
  padding: 15px;
  text-align: left;
  font-weight: 600;
  color: #333;
  border-bottom: 2px solid #e1e5e9;
}

.orders-table td {
  padding: 15px;
  border-bottom: 1px solid #f0f0f0;
  vertical-align: top;
}

.order-row:hover {
  background: #f8f9ff;
}

.customer-info {
  line-height: 1.4;
}

.customer-details {
  font-size: 0.9em;
  color: #666;
  margin-top: 5px;
}

.amount {
  font-weight: 600;
  color: #28a745;
  font-size: 1.1em;
}

.status-badge {
  padding: 6px 12px;
  border-radius: 20px;
  font-size: 0.8em;
  font-weight: 600;
  text-transform: uppercase;
}

.status-badge.pending {
  background: #fff3cd;
  color: #856404;
}

.status-badge.processing {
  background: #cce5ff;
  color: #004085;
}

.status-badge.paid {
  background: #d4edda;
  color: #155724;
}

.status-badge.failed {
  background: #f8d7da;
  color: #721c24;
}

.status-badge.cancelled {
  background: #e2e3e5;
  color: #383d41;
}

.payment-method {
  font-size: 0.9em;
  color: #666;
}

.date-info {
  line-height: 1.3;
}

.time {
  font-size: 0.9em;
  color: #666;
}

.actions {
  display: flex;
  gap: 8px;
  flex-wrap: wrap;
}

.btn {
  display: inline-block;
  padding: 8px 16px;
  border: none;
  border-radius: 6px;
  font-size: 12px;
  font-weight: 600;
  text-decoration: none;
  cursor: pointer;
  transition: all 0.3s ease;
}

.btn-sm {
  padding: 6px 12px;
  font-size: 11px;
}

.btn-primary {
  background: #667eea;
  color: white;
}

.btn-success {
  background: #28a745;
  color: white;
}

.btn-danger {
  background: #dc3545;
  color: white;
}

.btn:hover {
  transform: translateY(-1px);
  box-shadow: 0 3px 10px rgba(0,0,0,0.2);
}

.no-orders {
  text-align: center;
  padding: 60px 20px;
  color: #666;
}

.no-orders p {
  font-size: 1.2em;
  margin: 0;
}

@media (max-width: 768px) {
  .dashboard-orders {
    padding: 15px;
  }
  
  .page-header h1 {
    font-size: 2em;
  }
  
  .stats-grid {
    grid-template-columns: 1fr;
  }
  
  .orders-table {
    font-size: 12px;
  }
  
  .orders-table th,
  .orders-table td {
    padding: 10px 8px;
  }
  
  .actions {
    flex-direction: column;
  }
}
</style>
