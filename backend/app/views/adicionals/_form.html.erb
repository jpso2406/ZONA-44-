<%= form_with(model: adicional) do |form| %>
  <% if adicional.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(adicional.errors.count, "error") %> prohibited this adicional from being saved:</h2>

      <ul>
        <% adicional.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :ingredientes, style: "display: block" %>
    <%= form.text_field :ingredientes %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
