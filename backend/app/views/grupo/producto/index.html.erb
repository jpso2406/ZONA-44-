<section class="productos-premium">

  <!-- HERO CON BUSCADOR -->
  <header class="hero">
    <h1>Elige tu comida favorita 🍔🍕🌭</h1>
    <input type="text" class="buscador" placeholder="🔍 Buscar en el menú...">
  </header>

  <!-- CATEGORÍAS -->
  <div class="categorias-scroll">
    <button>🍔 Hamburguesas</button>
    <button>🍕 Pizzas</button>
    <button>🌭 Perros</button>
    <button>🥤 Bebidas</button>
    <button>🍟 Snacks</button>
    <button>🥗 Ensaladas</button>
  </div>

  <!-- GRID DE PRODUCTOS -->
  <div class="contenedor-productos">
    <% @productos.each do |producto| %>
      <div class="tarjeta-producto">
        <div class="imagen-wrapper">
          <% if producto.foto.attached? %>
            <%= image_tag producto.foto, alt: producto.name, class: "producto-imagen" %>
          <% else %>
            <%= image_tag "sin_imagen.png", alt: t("productos.sin_imagen"), class: "producto-imagen" %>
          <% end %>
        </div>
        <div class="info">
          <h2><%= producto.name %></h2>
          <p class="precio">$<%= producto.precio %></p>
          <%= link_to "Agregar ", grupo_producto_path(@grupo, producto), class: "btn-agregar" %>
        </div>
      </div>
    <% end %>
  </div>
</section>


<style>
body {
  margin: 0;
  font-family: 'Poppins', sans-serif;
  background: #fafafa;
  color: #333;
}

/* HERO */
.hero {
  text-align: center;
  padding: 2rem 1rem;
  background: linear-gradient(135deg, #ff7043, #ff5722);
  color: white;
}
.hero h1 {
  font-size: 1.6rem;
  margin-bottom: 1rem;
}
.buscador {
  width: 80%;
  max-width: 420px;
  padding: .7rem 1rem;
  border: none;
  border-radius: 30px;
  outline: none;
  font-size: .9rem;
}

/* CATEGORÍAS SCROLL */
.categorias-scroll {
  display: flex;
  gap: .7rem;
  overflow-x: auto;
  padding: .8rem 1rem;
  background: #fff;
  border-bottom: 1px solid #eee;
}
.categorias-scroll button {
  flex: 0 0 auto;
  padding: .4rem .9rem;
  border: none;
  border-radius: 18px;
  background: #f5f5f5;
  cursor: pointer;
  font-weight: 600;
  transition: background .3s;
  font-size: .85rem;
}
.categorias-scroll button:hover {
  background: #ff5722;
  color: white;
}

/* PRODUCTOS */
.contenedor-productos {
  display: flex;                /* 👈 usamos flexbox */
  flex-wrap: wrap;              /* permite varias filas */
  justify-content: center;      /* centra si hay pocos */
  gap: 1.2rem;
  padding: 2rem;
}

.tarjeta-producto {
  background: white;
  border-radius: 12px;
  box-shadow: 0 4px 10px rgba(0,0,0,0.08);
  overflow: hidden;
  transition: transform .3s ease, box-shadow .3s ease;
  width: 180px;                 /* 👈 ancho fijo */
  height: 260px;                /* 👈 altura fija */
  display: flex;
  flex-direction: column;
  justify-content: space-between;
}
.tarjeta-producto:hover {
  transform: translateY(-4px);
  box-shadow: 0 8px 16px rgba(0,0,0,0.15);
}

.imagen-wrapper {
  width: 100%;
  height: 140px;                /* 👈 tamaño fijo para imagen */
  overflow: hidden;
}
.producto-imagen {
  width: 100%;
  height: 100%;
  object-fit: cover;
  transition: transform .4s;
}
.tarjeta-producto:hover .producto-imagen {
  transform: scale(1.05);
}

.info {
  padding: .6rem;
  text-align: center;
  flex-grow: 1;
}
.info h2 {
  font-size: .9rem;
  margin-bottom: .3rem;
  line-height: 1.2;
  height: 2.4em;               /* 👈 fija altura del título */
  overflow: hidden;
  text-overflow: ellipsis;
  display: -webkit-box;
  -webkit-line-clamp: 2;       /* máximo 2 líneas */
  -webkit-box-orient: vertical;
}
.precio {
  color: #ff5722;
  font-weight: bold;
  font-size: .85rem;
  margin-bottom: .4rem;
}
.btn-agregar {
  display: inline-block;
  padding: .35rem .8rem;
  background: #ff5722;
  color: white;
  border-radius: 16px;
  text-decoration: none;
  font-weight: 600;
  font-size: .8rem;
  transition: background .3s;
}
.btn-agregar:hover {
  background: #e64a19;
}
</style>
