<link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.5.0/css/all.min.css">

<!-- Loader -->
<div id="loader" class="loader-container">
  <img src="<%= asset_path('animaciones/fire-loader.gif') %>" alt="Cargando..." class="fire-loader">
</div>

<div class="modal-login">
  <!-- 🔄 Switch de tema -->
  <div class="theme-toggle-switch">
    <input type="checkbox" id="theme-toggle" aria-label="Alternar entre modo oscuro y claro" />
    <label for="theme-toggle" class="toggle-label">
      <span class="toggle-ball" aria-hidden="true"></span>
      <span class="icon sun" aria-hidden="true">☀️</span>
      <span class="icon moon" aria-hidden="true">🌙</span>
    </label>
  </div>

  <div class="login-wrapper">
    <div class="login-container p-5 rounded-5 shadow-lg">
      <h2 class="login-title text-center mb-4 text-warning">🔐 <%= t("devise.login.title") %></h2>

      <%= form_for(resource, as: resource_name, url: session_path(resource_name), html: { class: "form-login", novalidate: true }) do |f| %>
        <div class="form-group mb-3">
          <%= f.label :email, t("devise.login.email"), class: "form-label" %>
          <%= f.email_field :email, autofocus: true, autocomplete: "email", class: "form-control", required: true %>
        </div>

        <div class="form-group mb-3">
            <label for="password" class="form-label">Contraseña</label>
            <div class="password-wrapper">
              <%= f.password_field :password, autocomplete: "current-password", class: "form-control", id: "password", required: true %>
             

            </div>
          </div>


        <% if devise_mapping.rememberable? %>
          <div class="form-check mb-3">
            <%= f.check_box :remember_me, class: "form-check-input" %>
            <%= f.label :remember_me, t("devise.login.remember_me"), class: "form-check-label" %>
          </div>
        <% end %>

        <%= f.submit t("devise.login.submit"), class: "btn-login btn btn-warning w-100 fw-bold mb-3", type: "submit" %>

        <p class="text-center">
          <%= link_to t("devise.login.forgot_password", default: "¿Olvidaste tu contraseña?"), new_password_path(resource_name), class: "text-warning fw-bold" %>
        </p>
      <% end %>

      <p class="text-center mt-2 text-light">¿No tienes cuenta? 
        <%= link_to "Registrarse", new_user_registration_path, class: "text-warning fw-bold" %>
      </p>
    </div>
  </div>
</div>

<% if flash[:alert].present? %>
  <div id="alert-box" class="alert-error" role="alert">
    ⚠️ <span><%= flash[:alert] %></span>
    <button type="button" class="alert-close" onclick="closeAlert()" aria-label="Cerrar alerta">✖</button>
  </div>
<% end %>

<script>
function togglePassword() {
  const passwordInput = document.getElementById("password");
  const toggleBtn = document.querySelector(".toggle-password i");
  if (passwordInput) {
    const isPassword = passwordInput.type === "password";
    passwordInput.type = isPassword ? "text" : "password";
    toggleBtn.className = isPassword ? "fa-solid fa-eye-slash" : "fa-solid fa-eye";
  }
}


function closeAlert() {
  const alertBox = document.getElementById("alert-box");
  if (alertBox) alertBox.remove();
}

document.addEventListener("turbo:load", function () {
  const loader = document.getElementById("loader");
  if (loader) {
    const hideLoader = () => loader.classList.add("hidden");
    setTimeout(hideLoader, 2000);
    window.addEventListener("load", hideLoader);
  }

  const body = document.body;
  const toggle = document.getElementById("theme-toggle");
  if (!toggle) return;

  const savedTheme = localStorage.getItem("theme");
  const isLight = savedTheme === "light";
  body.classList.toggle("light-mode", isLight);
  toggle.checked = isLight;

  toggle.addEventListener("change", function () {
    const isLight = toggle.checked;
    body.classList.toggle("light-mode", isLight);
    localStorage.setItem("theme", isLight ? "light" : "dark");
  });

  setTimeout(closeAlert, 5000);
});
</script>
<style>
.modal-login {
  min-height: 100vh;
  display: flex;
  flex-direction: column;
  justify-content: center;
  background: linear-gradient(to right, #1e1e2f, #2c2c3c);
  padding: 20px;
}

.login-wrapper {
  display: flex;
  justify-content: center;
  align-items: center;
}

.login-container {
  background: #2c2c3c;
  border-radius: 20px;
  box-shadow: 0 10px 30px rgba(0,0,0,0.6);
  max-width: 400px;
  width: 100%;
  padding: 2.5rem;
  transition: transform 0.3s ease, box-shadow 0.3s ease;
}

.login-container:hover {
  transform: translateY(-5px);
  box-shadow: 0 15px 40px rgba(0,0,0,0.7);
}

.form-group {
  position: relative;
}

.form-label {
  display: block;
  margin-bottom: 6px;
  color: #fff;
  font-weight: 500;
  font-size: 0.95rem;
}

.form-control {
  background: #1e1e2f;
  border: none;
  border-radius: 10px;
  color: #fff;
  padding: 12px 15px;
  width: 100%;
  transition: background 0.3s ease, box-shadow 0.3s ease;
}

.form-control:focus {
  background: #252538;
  outline: none;
  box-shadow: 0 0 0 3px rgba(255, 193, 7, 0.4);
}

.password-container .toggle-password {
  position: absolute;
  top: 50%;
  right: 15px;
  transform: translateY(-50%);
  background: none;
  border: none;
  cursor: pointer;
  font-size: 1.2rem;
  color: #ffc107;
  user-select: none;
  padding: 0;
  line-height: 1;
  transition: color 0.3s ease;
}

.password-container .toggle-password:hover {
  color: #fff;
}

.btn-login {
  font-size: 1rem;
  padding: 12px;
  border-radius: 12px;
}

.alert-error {
  position: fixed;
  top: 20px;
  left: 50%;
  transform: translateX(-50%);
  background: #ffe0e0;
  padding: 12px 20px;
  border-radius: 12px;
  color: #a30000;
  display: flex;
  align-items: center;
  gap: 12px;
  z-index: 9999;
  box-shadow: 0 8px 20px rgba(255,0,0,0.2);
  max-width: 90vw;
}

.alert-close {
  margin-left: auto;
  background: none;
  border: none;
  cursor: pointer;
  font-weight: bold;
  color: #a30000;
  padding: 0 5px;
}

.alert-close:hover {
  color: #000;
}
.form-label {
  display: block;
  margin-bottom: 6px;
  color: #fff;
  font-weight: 500;
  font-size: 0.95rem;
}

.password-wrapper {
  position: relative;
}

.password-wrapper .form-control {
  width: 100%;
  padding-right: 40px; /* espacio para el botón 👁️ */
}

.toggle-password {
  position: absolute;
  top: 50%;
  right: 12px;
  transform: translateY(-50%);
  background: none;
  border: none;
  cursor: pointer;
  font-size: 1.2rem;
  color: #ffc107;
  user-select: none;
  padding: 0;
  line-height: 1;
  transition: color 0.3s ease;
}

.toggle-password:hover {
  color: #fff;
}

 
</style>